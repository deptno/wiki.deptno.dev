{
  "name": "@app/meilisearch-updater",
  "version": "2.0.0",
  "main": "dist",
  "type": "module",
  "scripts": {
    "dev": "pnpm build && node --enable-source-maps dist",
    "start": "node --enable-source-maps dist",
    "build": "tsc",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "20",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "meilisearch": "^0.32.4"
  }
}
